#include <stdio.h>
    // segundos pra hora = 1 * 3600, minutos pra hora = 1 * 60
    
void converte_hora(int total_segundos, int *hora, int *min, int *seg)
    {

    }


    int main(){
        int total_segundos;
        total_segundos >= 0;
            scanf("%d", &total_segundos);
            converte_hora(total_segundos, hora, min, seg);

    }